# PyTorch는 CUDA 빌드 인덱스에서 받는다 (GPU 없는 환경이면 이 줄을 지우고
# `pip install torch torchvision`으로 CPU 빌드를 설치할 것)
--extra-index-url https://download.pytorch.org/whl/cu126

opencv-python==5.0.0.93
numpy==2.2.6
pytest==9.1.1
matplotlib==3.10.9
torch==2.14.0+cu126
torchvision==0.29.0+cu126
icrawler==0.6.10
Pillow==12.3.0
timm==1.0.29
